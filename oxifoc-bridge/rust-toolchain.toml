[toolchain]
# Pinned like the STM32 crates — a floating "nightly" makes CI break on any
# upstream nightly regression and builds non-reproducible.
channel    = "nightly-2026-03-22"
components = ["rust-src"]
targets = ["riscv32imac-unknown-none-elf"]
