[package]
name = "oxifoc-f405"
version = "0.1.0"
edition = "2024"
description = "STM32F405-based motor controller (Simple FOCer 2 / VESC hardware)"

[[bin]]
name = "oxifoc-f405"
path = "src/main.rs"
test = false
bench = false

[features]
default = ["transport-usb"]
# USB bulk endpoints via embassy-usb; defmt over RTT for debugging.
transport-usb = ["ergot/embassy-usb-v0_6", "ergot/defmt-sink-rtt"]

[dependencies]
# Embassy + Cortex-M
embassy-executor = { version = "0.10.0", features = ["platform-cortex-m", "executor-thread", "executor-interrupt"] }
embassy-futures = { version = "0.1.2" }
embassy-stm32 = { version = "0.6.0", features = ["stm32f405rg", "time-driver-tim2", "unstable-pac", "exti", "defmt"] }
embassy-sync = { version = "0.8.0" }
embassy-time = { version = "0.5.1", features = ["tick-hz-32_768"] }
embassy-usb = { version = "0.6.0" }

cortex-m = { version = "0.7.7", features = ["inline-asm", "critical-section-single-core"] }
cortex-m-rt = "0.7.5"
critical-section = "1.2"

# Debugging and logging
defmt = "1.0.1"
panic-probe = { version = "1.0.0", features = ["print-defmt"] }

# Utilities
heapless = { version = "0.9.2", default-features = false }
mutex = "1.0.2"
static_cell = "2.1"
assign-resources = "0.5.0"

# Gate driver
drv8301-dd = { version = "0.2.0", features = ["defmt"] }
embedded-hal-bus = "0.3"

# Persistent configuration storage
sequential-storage = { version = "7.1", features = ["postcard", "defmt"] }
embedded-storage-async = "0.4"
embassy-embedded-hal = { version = "0.6.0" }
postcard = "1.1"

# Local crates
oxifoc-core = { path = "../oxifoc-core", features = ["defmt", "runtime", "storage"] }
ergot = { git = "https://github.com/okhsunrog/ergot", branch = "liveness-tracking-and-state-notifications", default-features = false, features = [
    "embassy-usb-v0_6",
    "disable-cache-padding",
    "defmt-sink-rtt",
] }

[profile.dev]
debug = true
lto = "fat"
opt-level = 3
incremental = true

[profile.release]
debug = 2
lto = "fat"
opt-level = 3
incremental = true

