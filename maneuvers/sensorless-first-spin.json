{
  "name": "sensorless-first-spin",
  "description": "First closed-loop sensorless spin (ZD2808, baked params): torque command from standstill exercises the cold-start state machine (align -> ramp -> observer handoff), hold, then unload. PSU-safe profile assumed (RampToZero, bus_regen=0).",
  "capture": {
    "fast_hz": 10000,
    "tail_s": 1.0
  },
  "terminal": "stop",
  "timeline": [
    { "t": 0.5, "cmd": { "start": { "iq": 1.5 } } },
    { "t": 4.5, "cmd": { "start": { "iq": 0.8 } } },
    { "t": 6.5, "cmd": { "start": { "iq": 0.0 } } }
  ]
}
