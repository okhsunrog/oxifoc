# Probe-rs runner configuration for device crate with custom defmt log formatting

[build]
target = "thumbv7em-none-eabihf"

[target.thumbv7em-none-eabihf]
runner = ["bash", "scripts/probe_run.sh"]

[env]
# Show all defmt levels unless overridden
DEFMT_LOG = "debug"
